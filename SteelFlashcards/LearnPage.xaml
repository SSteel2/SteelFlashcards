<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SteelFlashcards.LearnPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteelFlashcards"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Unloaded="Page_Unloaded">

    <Page.Resources>
        <local:BoolToIconConverter x:Key="BoolToIcon"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition />
            <ColumnDefinition Width="360"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left pane -->
        <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="4" Padding="16" Spacing="8">
            <Grid Background="{StaticResource CardStrokeColorDefault}" Padding="20,12,20,20" CornerRadius="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="36"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" BorderThickness="0,0,0,1" BorderBrush="Black" Margin="0,0,0,8" Padding="0,0,0,4">
                    <TextBlock Text="Words" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="16"/>
                </Border>
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Total:" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Grid.Row="1" Text="{x:Bind ViewModel.WordCount, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="0" Grid.Row="2" Text="Mastered:" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Grid.Row="2" Text="{x:Bind ViewModel.MasteryCount, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <FontIcon Grid.Column="2" Grid.Row="1" Grid.RowSpan="2" Glyph="&#xE735;"
                          FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="40" Foreground="Gold"
                          Visibility="{x:Bind ViewModel.AllWordsMastered, Mode=OneWay, Converter={StaticResource BoolToVis}}"
                          HorizontalAlignment="Right"/>
            </Grid>
            <Grid Background="{StaticResource CardStrokeColorDefault}" Padding="20,12,20,20" CornerRadius="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="Black" Margin="0,0,0,8" Padding="0,0,0,4">
                    <TextBlock Text="Tags" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="16"/>
                </Border>
                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Tags}" SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="MinHeight" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Spacing="8"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:DictionaryTag">
                            <ItemContainer>
                                <Border BorderThickness="1" BorderBrush="Black" CornerRadius="12" Padding="8,2">
                                    <StackPanel Orientation="Horizontal" Spacing="4" Width="Auto">
                                        <TextBlock Text="{x:Bind TagName}"/>
                                        <Border BorderThickness="1,0,0,0" BorderBrush="Black" Padding="4,0,0,0">
                                            <TextBlock Text="{x:Bind WordCount}"/>
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </ItemContainer>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </StackPanel>
        
        <!-- Center content -->
        <Grid Grid.Column="1" Grid.Row="0" Background="{StaticResource CardStrokeColorDefault}" Margin="80,60,80,20" Padding="20" CornerRadius="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto" MinWidth="83"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Previous Word" Grid.Row="0" Grid.Column="0" VerticalAlignment="Bottom"/>
            <TextBlock Text="{x:Bind ViewModel.LastAnswer.Word, Mode=OneWay}" Grid.Row="1" Grid.RowSpan="3" Grid.Column="0" FontSize="40"/>
            <TextBlock Text="Your Answer" Grid.Row="0" Grid.Column="1" VerticalAlignment="Bottom"/>
            <TextBlock Text="{x:Bind ViewModel.LastAnswer.Guess, Mode=OneWay}" Grid.Row="1" Grid.Column="1" FontSize="24"/>
            <TextBlock Text="Correct Answer" Grid.Row="2" Grid.Column="1" VerticalAlignment="Bottom"/>
            <TextBlock Text="{x:Bind ViewModel.LastAnswer.CorrectAnswer, Mode=OneWay}" Grid.Row="3" Grid.Column="1" FontSize="24"/>
            <TextBlock Text="{x:Bind ViewModel.LastAnswer.IsCorrect, Mode=OneWay, Converter={StaticResource BoolToIcon}}" Grid.Row="0" Grid.Column="2" Grid.RowSpan="4" FontSize="60" VerticalAlignment="Center"/>
        </Grid>
        <!--TODO: Focus on answer box on navigation to this page-->
        <TextBlock Grid.Column="1" Grid.Row="1" Name="WordBlock" Text="{x:Bind ViewModel.CurrentWord, Mode=OneWay}" FontSize="80" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBox Name="GuessBox" PlaceholderText="meaning" FontSize="40" MinWidth="600" KeyUp="GuessBox_KeyUp"/>
            <Button Command="{x:Bind ViewModel.AcceptAnswerCommand}" CommandParameter="{x:Bind GuessBox.Text, Mode=OneWay}" VerticalAlignment="Stretch" Margin="8,0,0,0" Width="66" Click="AcceptButton_Click">
                <SymbolIcon Symbol="Forward"/>
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Enter"/>
                </Button.KeyboardAccelerators>
            </Button>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
            <Button Content="Reset" Command="{x:Bind ViewModel.NewAnswersCommand}" Name="NewButton"/>
        </StackPanel>
        
        <!-- Right pane -->
        <!-- TODO: Some static resource background color -->
        <ListView Grid.Column="2" Grid.Row="0" Grid.RowSpan="4" x:Name="AnswersListView" Background="LightGray" ItemsSource="{x:Bind ViewModel.Answers}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="MinHeight" Value="0"/>
                    <Setter Property="Padding" Value="16,4,12,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Margin="16,8,12,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="36"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="36"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="#" Grid.Column="0" FontWeight="Bold"/>
                        <TextBlock Text="Word" Grid.Column="1" FontWeight="Bold"/>
                        <TextBlock Text="Guess" Grid.Column="2" FontWeight="Bold"/>
                        <TextBlock Text="Answer" Grid.Column="3" FontWeight="Bold"/>
                        <TextBlock Text="Score" Grid.Column="4" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:LearnPageAnswer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="36"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="36"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Path=Order}" Grid.Column="0"/>
                        <TextBlock Text="{x:Bind Path=Word}" Grid.Column="1"/>
                        <TextBlock Text="{x:Bind Path=Guess}" Grid.Column="2"/>
                        <TextBlock Text="{x:Bind Path=CorrectAnswer}" Grid.Column="3"/>
                        <TextBlock Text="{x:Bind Path=IsCorrect, Converter={StaticResource BoolToIcon}}" Grid.Column="4" HorizontalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
